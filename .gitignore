# Python
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.egg-info/
backend/.pytest_cache/
backend/.env
backend/.env.*
!backend/.env.example

# Next.js
frontend/.next/
frontend/node_modules/
frontend/.env.local
frontend/.env*.local
frontend/tsconfig.tsbuildinfo

# macOS
.DS_Store

# Personal config (never commit)
jarvis.config.ts
*.local.ts
JarvisNative/.build/
# Compiled .app bundle — rebuild from `swift build` locally; never commit binaries.
JarvisNative/JarvisNative.app/

# Claude Code editor state (worktrees, launch configs, etc.)
.claude/

# Generic secret files — belt + suspenders
*.pem
*.key
*_token.txt
.jarvis/

# Personal scraped data — academic records, AnKing card content, etc.
# uworld_stub.json is the shipped placeholder; everything else in storage/ is private.
backend/storage/uworld_history.json
backend/storage/anki_qid_index.json
backend/storage/anki_qid_cache.json
